name: CICD_production

on:
  pull_request:
    types: [closed]
    branches:
      - main
    paths-ignore:
      - website/**
  workflow_dispatch:

concurrency:
  group: cicd-production
  cancel-in-progress: false

permissions:
  contents: write

jobs:
  prepare:
    runs-on: ubuntu-latest
    environment: production
    if: |
      (github.event_name == 'workflow_dispatch' && github.ref == 'refs/heads/main') ||
      (github.event_name == 'pull_request' && github.event.pull_request.merged == true)
    env:
      CARGO_INCREMENTAL: 0
    outputs:
      deploy_sha: ${{ steps.auto_bump_base.outputs.deploy_sha }}

    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
          ref: ${{ github.event_name == 'pull_request' && github.event.pull_request.merge_commit_sha || github.sha }}

      - name: Auto bump base version when Dockerfile.base changes
        id: auto_bump_base
        working-directory: .
        run: |
          set -euo pipefail
          trigger_sha="${{ github.event_name == 'pull_request' && github.event.pull_request.merge_commit_sha || github.sha }}"

          if [[ "${{ github.event_name }}" == "pull_request" ]]; then
            target_branch="${{ github.event.pull_request.base.ref }}"
          else
            target_branch="${{ github.ref_name }}"
          fi
          [[ -n "$target_branch" ]] || { echo "Unable to resolve target branch" >&2; exit 1; }

          base_file="Dockerfile.base"
          version_file="version_base.txt"
          md5_file="version_base.md5"

          [[ -f "$base_file" ]] || { echo "Missing $base_file" >&2; exit 1; }
          [[ -f "$version_file" ]] || { echo "Missing $version_file" >&2; exit 1; }

          current_md5="$(md5sum "$base_file" | awk '{print $1}')"
          previous_md5=""
          if [[ -f "$md5_file" ]]; then
            previous_md5="$(tr -d '[:space:]' < "$md5_file")"
          fi

          current_version="$(tr -d '[:space:]' < "$version_file")"
          [[ "$current_version" =~ ^[0-9]+\.[0-9]+\.[0-9]+$ ]] || {
            echo "Invalid version in $version_file: $current_version" >&2
            exit 1
          }

          if [[ "$current_md5" != "$previous_md5" ]]; then
            IFS='.' read -r major minor patch <<< "$current_version"
            new_version="${major}.${minor}.$((patch + 1))"
            echo "$new_version" > "$version_file"
            echo "$current_md5" > "$md5_file"

            git config user.name "github-actions[bot]"
            git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
            git add "$version_file" "$md5_file"
            git commit -m "chore(ci): bump base version to $new_version [skip ci]"
            git push origin "HEAD:$target_branch"
            deploy_sha="$(git rev-parse HEAD)"
            echo "deploy_sha=$deploy_sha" >> "$GITHUB_OUTPUT"

            echo "Base Dockerfile changed: bumped version_base.txt to $new_version (deploy_sha=$deploy_sha)"
          else
            echo "deploy_sha=$trigger_sha" >> "$GITHUB_OUTPUT"
            echo "Base Dockerfile unchanged: keep version_base.txt at $current_version"
          fi

  test:
    runs-on: ubuntu-latest
    needs: prepare
    environment: production
    env:
      CARGO_INCREMENTAL: 0

    defaults:
      run:
        working-directory: DoWhiz_service

    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          fetch-depth: 1
          ref: ${{ needs.prepare.outputs.deploy_sha }}

      - name: Reclaim runner disk space before Rust restore
        run: |
          set -euo pipefail
          echo "Disk usage before cleanup:"
          df -h
          sudo rm -rf \
            /usr/local/lib/android \
            /usr/local/.ghcup \
            /opt/ghc \
            /opt/hostedtoolcache/CodeQL \
            /usr/share/dotnet \
            /usr/share/swift
          sudo apt-get clean
          echo "Disk usage after cleanup:"
          df -h

      - name: Restore Rust cache
        uses: Swatinem/rust-cache@v2
        with:
          workspaces: |
            DoWhiz_service -> target

      - name: Drop cached artifacts not needed for tests
        run: |
          set -euo pipefail
          rm -rf target/debug/incremental target/release
          echo "Disk usage after pruning Rust artifacts:"
          df -h .

      - name: Run tests (scheduler_module)
        run: cargo test --locked -p scheduler_module
        continue-on-error: true

      - name: Run tests (run_task_module)
        run: cargo test --locked -p run_task_module
        continue-on-error: true

  build:
    runs-on: ubuntu-latest
    needs:
      - prepare
      - test
    environment: production
    env:
      CARGO_INCREMENTAL: 0

    defaults:
      run:
        working-directory: DoWhiz_service

    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          fetch-depth: 1
          ref: ${{ needs.prepare.outputs.deploy_sha }}

      - name: Reclaim runner disk space before Rust restore
        run: |
          set -euo pipefail
          echo "Disk usage before cleanup:"
          df -h
          sudo rm -rf \
            /usr/local/lib/android \
            /usr/local/.ghcup \
            /opt/ghc \
            /opt/hostedtoolcache/CodeQL \
            /usr/share/dotnet \
            /usr/share/swift
          sudo apt-get clean
          echo "Disk usage after cleanup:"
          df -h

      - name: Restore Rust cache
        uses: Swatinem/rust-cache@v2
        with:
          workspaces: |
            DoWhiz_service -> target

      - name: Remove cached debug artifacts before release build
        run: rm -rf target/debug

      - name: Build release binaries
        run: |
          cargo build --locked -p scheduler_module --release \
            --bin rust_service \
            --bin inbound_fanout \
            --bin inbound_gateway \
            --bin google-docs \
            --bin google-sheets \
            --bin google-slides \
            --bin set_postmark_inbound_hook \
            --bin notion_api_cli \
            --bin tpm_cli \
            --bin slack_cli \
            --bin discord_cli \
            --bin lark_cli \
            --bin identity_lookup_cli \
            --bin grocery_cli \
            --bin task_status_cli

      - name: Archive rust binaries
        uses: actions/upload-artifact@v4
        with:
          name: rust-binaries
          compression-level: 0
          path: |
            DoWhiz_service/target/release/rust_service
            DoWhiz_service/target/release/inbound_fanout
            DoWhiz_service/target/release/inbound_gateway
            DoWhiz_service/target/release/google-docs
            DoWhiz_service/target/release/google-sheets
            DoWhiz_service/target/release/google-slides
            DoWhiz_service/target/release/set_postmark_inbound_hook
            DoWhiz_service/target/release/notion_api_cli
            DoWhiz_service/target/release/tpm_cli
            DoWhiz_service/target/release/slack_cli
            DoWhiz_service/target/release/discord_cli
            DoWhiz_service/target/release/lark_cli
            DoWhiz_service/target/release/identity_lookup_cli
            DoWhiz_service/target/release/grocery_cli
            DoWhiz_service/target/release/task_status_cli
          if-no-files-found: error

  deploy:
    runs-on: ubuntu-latest
    needs:
      - prepare
      - build
    environment: production
    if: |
      (github.event_name == 'workflow_dispatch' && github.ref == 'refs/heads/main') ||
      (github.event_name == 'pull_request' && github.event.pull_request.merged == true)

    steps:
      - name: Checkout deploy source
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
          ref: ${{ needs.prepare.outputs.deploy_sha }}

      - name: Download rust binaries
        uses: actions/download-artifact@v4
        with:
          name: rust-binaries
          path: ./rust-binaries

      - name: Create SSH Directory
        run: |
          mkdir -p ~/.ssh
          chmod 700 ~/.ssh

      - name: Decode SSH Private Key
        run: |
          echo "${{ secrets.PROD_SSH_PRIVATE_KEY }}" > ~/.ssh/private_key.pem
          chmod 600 ~/.ssh/private_key.pem

      - name: Add Azure VM to known_hosts
        run: |
          ssh-keyscan -H ${{ secrets.PROD_AZURE_VM_IP }} >> ~/.ssh/known_hosts

      - name: Create SSH config file
        run: |
          {
            echo "Host azureproduction"
            echo "  HostName ${{ secrets.PROD_AZURE_VM_IP }}"
            echo "  User ${{ secrets.PROD_AZURE_VM_USER }}"
            echo "  Port 22"
            echo "  IdentityFile ~/.ssh/private_key.pem"
          } >> ~/.ssh/config

      - name: Update repo on Azure VM
        run: |
          ssh -F ~/.ssh/config azureproduction << 'EOF'
          TARGET_SHA="${{ needs.prepare.outputs.deploy_sha }}"
          [[ -n "$TARGET_SHA" ]] || {
            echo "Missing TARGET_SHA" >&2
            exit 1
          }
          cd /home/${{ secrets.PROD_AZURE_VM_USER }}/server/DoWhiz
          git fetch origin --prune
          git checkout -f
          git checkout -f "$TARGET_SHA"
          mkdir -p /home/${{ secrets.PROD_AZURE_VM_USER }}/server/DoWhiz/DoWhiz_service/target/release
          EOF

      - name: Ensure release directory is writable
        run: |
          ssh -F ~/.ssh/config azureproduction << 'EOF'
          set -e
          RELEASE_DIR=/home/${{ secrets.PROD_AZURE_VM_USER }}/server/DoWhiz/DoWhiz_service/target/release
          mkdir -p "$RELEASE_DIR"
          chmod u+rwx "$RELEASE_DIR"
          EOF

      - name: Upload rust binaries to Azure VM (tmp)
        run: |
          ssh -F ~/.ssh/config azureproduction "mkdir -p /tmp/dowhiz-release"
          scp -F ~/.ssh/config ./rust-binaries/* azureproduction:/tmp/dowhiz-release/

      - name: Install rust binaries on Azure VM
        run: |
          ssh -F ~/.ssh/config azureproduction << 'EOF'
          set -e
          RELEASE_DIR=/home/${{ secrets.PROD_AZURE_VM_USER }}/server/DoWhiz/DoWhiz_service/target/release
          install -m 755 /tmp/dowhiz-release/rust_service "$RELEASE_DIR/rust_service"
          install -m 755 /tmp/dowhiz-release/inbound_fanout "$RELEASE_DIR/inbound_fanout"
          install -m 755 /tmp/dowhiz-release/inbound_gateway "$RELEASE_DIR/inbound_gateway"
          install -m 755 /tmp/dowhiz-release/google-docs "$RELEASE_DIR/google-docs"
          install -m 755 /tmp/dowhiz-release/google-sheets "$RELEASE_DIR/google-sheets"
          install -m 755 /tmp/dowhiz-release/google-slides "$RELEASE_DIR/google-slides"
          install -m 755 /tmp/dowhiz-release/set_postmark_inbound_hook "$RELEASE_DIR/set_postmark_inbound_hook"
          install -m 755 /tmp/dowhiz-release/notion_api_cli "$RELEASE_DIR/notion_api_cli"
          install -m 755 /tmp/dowhiz-release/tpm_cli "$RELEASE_DIR/tpm_cli"
          install -m 755 /tmp/dowhiz-release/slack_cli "$RELEASE_DIR/slack_cli"
          install -m 755 /tmp/dowhiz-release/discord_cli "$RELEASE_DIR/discord_cli"
          install -m 755 /tmp/dowhiz-release/lark_cli "$RELEASE_DIR/lark_cli"
          install -m 755 /tmp/dowhiz-release/identity_lookup_cli "$RELEASE_DIR/identity_lookup_cli"
          install -m 755 /tmp/dowhiz-release/grocery_cli "$RELEASE_DIR/grocery_cli"
          install -m 755 /tmp/dowhiz-release/task_status_cli "$RELEASE_DIR/task_status_cli"
          rm -rf /tmp/dowhiz-release
          EOF

      - name: Write production .env on Azure VM
        run: |
          ssh -F ~/.ssh/config azureproduction << 'EOF'
          set -e
          APP_DIR=/home/${{ secrets.PROD_AZURE_VM_USER }}/server/DoWhiz/DoWhiz_service
          mkdir -p "$APP_DIR"
          COMMON_ENV_FILE="$APP_DIR/.env.common.tmp"
          TARGET_ENV_FILE="$APP_DIR/.env.target.tmp"
          cat << 'ENV_COMMON_EOF' > "$COMMON_ENV_FILE"
          ${{ secrets.ENV_COMMON }}
          ENV_COMMON_EOF
          cat << 'ENV_TARGET_EOF' > "$TARGET_ENV_FILE"
          ${{ secrets.ENV_PROD }}
          ENV_TARGET_EOF

          ENV_FILE="$APP_DIR/.env"
          # Merge key/value pairs with target env taking precedence over common env.
          awk '
            function trim(value) {
              gsub(/^[[:space:]]+|[[:space:]]+$/, "", value);
              return value;
            }
            {
              line = $0;
              sub(/\r$/, "", line);
              if (line ~ /^[[:space:]]*($|#)/) next;
              if (line ~ /^export[[:space:]]+/) sub(/^export[[:space:]]+/, "", line);
              pos = index(line, "=");
              if (pos == 0) next;
              key = trim(substr(line, 1, pos - 1));
              if (key == "") next;
              value = substr(line, pos + 1);
              if (!(key in seen)) {
                seen[key] = 1;
                order[++count] = key;
              }
              env[key] = value;
            }
            END {
              for (i = 1; i <= count; i++) {
                key = order[i];
                print key "=" env[key];
              }
            }
          ' "$COMMON_ENV_FILE" "$TARGET_ENV_FILE" > "$ENV_FILE"
          rm -f "$COMMON_ENV_FILE" "$TARGET_ENV_FILE"

          if grep -Eq '^(STAGING_|PROD_)' "$ENV_FILE"; then
            echo "ERROR: Prefixed env keys are not allowed in deployment .env" >&2
            grep -En '^(STAGING_|PROD_)' "$ENV_FILE" >&2 || true
            exit 1
          fi

          # Non-secret production override: keep Oliver email ingestion resilient even
          # when Gmail routing/forwarding does not copy messages to Postmark inbound.
          cat << GMAIL_POLLER_ENV_EOF >> "$ENV_FILE"
          GMAIL_POLLER_ENABLED=true
          GMAIL_POLLER_RECIPIENT=oliver@dowhiz.com
          GMAIL_POLLER_INTERVAL_SECS=15
          GMAIL_POLLER_LOOKBACK_DAYS=2
          GMAIL_POLLER_MAX_RESULTS=50
          GMAIL_POLLER_STATE_PATH=$APP_DIR/.gmail_poller_processed_ids
          GMAIL_POLLER_BOOTSTRAP_PROCESS_EXISTING=false
          GMAIL_POLLER_ENV_EOF
          chmod 600 "$APP_DIR/.env"
          EOF

          # Append Kroger API credentials
          ssh -F ~/.ssh/config azureproduction << EOF
          echo "KROGER_CLIENT_ID=${{ secrets.KROGER_CLIENT_ID }}" >> /home/${{ secrets.PROD_AZURE_VM_USER }}/server/DoWhiz/DoWhiz_service/.env
          echo "KROGER_CLIENT_SECRET=${{ secrets.KROGER_CLIENT_SECRET }}" >> /home/${{ secrets.PROD_AZURE_VM_USER }}/server/DoWhiz/DoWhiz_service/.env
          EOF

      - name: Disable legacy service before ACI build
        run: |
          ssh -F ~/.ssh/config azureproduction << 'EOF'
          set -e

          if systemctl list-unit-files 'dowhiz-oliver.service' --no-legend 2>/dev/null | grep -q '^dowhiz-oliver\.service'; then
            sudo systemctl disable --now dowhiz-oliver.service || true
          fi
          EOF

      - name: Resolve ACI build configuration
        id: aci_config
        env:
          ENV_COMMON: ${{ secrets.ENV_COMMON }}
          ENV_TARGET: ${{ secrets.ENV_PROD }}
        run: |
          set -euo pipefail
          common_env="${ENV_COMMON:-}"
          target_env="${ENV_TARGET:-}"

          read_env_value() {
            local target_key="$1"
            local value
            value="$(
              {
                printf '%s\n' "$common_env"
                printf '%s\n' "$target_env"
              } | awk -v wanted="$target_key" '
                function trim(s) {
                  gsub(/^[[:space:]]+|[[:space:]]+$/, "", s)
                  return s
                }
                {
                  line = $0
                  sub(/\r$/, "", line)
                  if (line ~ /^[[:space:]]*($|#)/) next
                  if (line ~ /^export[[:space:]]+/) sub(/^export[[:space:]]+/, "", line)
                  pos = index(line, "=")
                  if (pos == 0) next
                  key = trim(substr(line, 1, pos - 1))
                  if (key == "") next
                  env[key] = substr(line, pos + 1)
                }
                END {
                  if (wanted in env) print env[wanted]
                }
              ' | tail -n1
            )"
            value="${value#\"}"
            value="${value%\"}"
            value="${value#\'}"
            value="${value%\'}"
            printf '%s' "$value"
          }

          run_task_backend="$(read_env_value RUN_TASK_EXECUTION_BACKEND)"
          deploy_target="$(read_env_value DEPLOY_TARGET)"
          should_build=0
          case "$run_task_backend" in
            azure_aci)
              should_build=1
              ;;
            auto)
              if [[ "$deploy_target" == "staging" || "$deploy_target" == "production" ]]; then
                should_build=1
              fi
              ;;
          esac

          if [[ "$should_build" -eq 0 ]]; then
            echo "should_build=0" >> "$GITHUB_OUTPUT"
            echo "Skipping ACI image build: RUN_TASK_EXECUTION_BACKEND=$run_task_backend DEPLOY_TARGET=$deploy_target"
            exit 0
          fi

          run_task_image="$(read_env_value RUN_TASK_AZURE_ACI_IMAGE)"
          [[ -n "$run_task_image" ]] || {
            echo "RUN_TASK_AZURE_ACI_IMAGE is required when Azure ACI backend is enabled" >&2
            exit 1
          }
          login_server="${run_task_image%%/*}"
          image_name="${run_task_image#*/}"
          if [[ "$login_server" == "$run_task_image" || -z "$image_name" ]]; then
            echo "Invalid RUN_TASK_AZURE_ACI_IMAGE: $run_task_image" >&2
            exit 1
          fi
          registry_name="${login_server%%.*}"
          [[ -n "$registry_name" ]] || {
            echo "Unable to parse ACR registry name from image: $run_task_image" >&2
            exit 1
          }
          [[ "$image_name" == *:* ]] || {
            echo "RUN_TASK_AZURE_ACI_IMAGE must include an explicit tag: $run_task_image" >&2
            exit 1
          }

          {
            echo "should_build=1"
            echo "run_task_image=$run_task_image"
            echo "login_server=$login_server"
            echo "image_name=$image_name"
            echo "registry_name=$registry_name"
          } >> "$GITHUB_OUTPUT"

      - name: Azure login for ACR build
        if: steps.aci_config.outputs.should_build == '1'
        uses: azure/login@v2
        with:
          creds: ${{ secrets.PROD_AZURE_CREDENTIALS != '' && secrets.PROD_AZURE_CREDENTIALS || secrets.AZURE_CREDENTIALS }}

      - name: Build and push ACI image on GitHub Runner
        if: steps.aci_config.outputs.should_build == '1'
        env:
          RUN_TASK_IMAGE: ${{ steps.aci_config.outputs.run_task_image }}
          LOGIN_SERVER: ${{ steps.aci_config.outputs.login_server }}
          IMAGE_NAME: ${{ steps.aci_config.outputs.image_name }}
          REGISTRY_NAME: ${{ steps.aci_config.outputs.registry_name }}
        run: |
          set -euo pipefail
          command -v az >/dev/null 2>&1 || { echo "Azure CLI not found on runner" >&2; exit 1; }

          APP_ROOT="$GITHUB_WORKSPACE"
          build_context_root="$(mktemp -d)"
          cleanup_build_context() {
            rm -rf "$build_context_root"
          }
          trap cleanup_build_context EXIT

          echo "Preparing clean ACR build context at $build_context_root"
          tar -C "$APP_ROOT" \
            --exclude='DoWhiz_service/target' \
            --exclude='DoWhiz_service/.workspace' \
            --exclude='DoWhiz_service/.env' \
            --exclude='DoWhiz_service/.env.*' \
            -cf - Dockerfile.aci Dockerfile.base version_base.txt .dockerignore DoWhiz_service | tar -C "$build_context_root" -xf -

          ctx_release_dir="$build_context_root/DoWhiz_service/target/release"
          mkdir -p "$ctx_release_dir"
          required_runtime_bins=(rust_service inbound_fanout inbound_gateway google-docs google-sheets google-slides notion_api_cli tpm_cli slack_cli discord_cli lark_cli identity_lookup_cli grocery_cli)
          for bin_name in "${required_runtime_bins[@]}"; do
            src_bin="$GITHUB_WORKSPACE/rust-binaries/$bin_name"
            [[ -f "$src_bin" ]] || {
              echo "Missing binary artifact required for ACI image: $src_bin" >&2
              ls -al "$GITHUB_WORKSPACE/rust-binaries" 2>/dev/null || true
              exit 1
            }
            install -m 755 "$src_bin" "$ctx_release_dir/$bin_name"
          done

          base_version_file="$build_context_root/version_base.txt"
          base_dockerfile="$build_context_root/Dockerfile.base"
          [[ -f "$base_version_file" ]] || {
            echo "Missing base version file: $base_version_file" >&2
            exit 1
          }
          [[ -f "$base_dockerfile" ]] || {
            echo "Missing base Dockerfile: $base_dockerfile" >&2
            exit 1
          }

          base_version="$(tr -d '[:space:]' < "$base_version_file")"
          [[ -n "$base_version" ]] || {
            echo "Base version file is empty: $base_version_file" >&2
            exit 1
          }
          base_md5="$(md5sum "$base_dockerfile" | awk '{print $1}')"
          base_md5_short="${base_md5:0:12}"
          run_task_repo="${IMAGE_NAME%:*}"
          base_repo="${run_task_repo}-base"
          base_tag="${base_version}-${base_md5_short}"
          base_image_name="${base_repo}:${base_tag}"
          base_image_full="${LOGIN_SERVER}/${base_image_name}"

          cd "$build_context_root"
          if az acr repository show --name "$REGISTRY_NAME" --image "$base_image_name" --only-show-errors >/dev/null 2>&1; then
            echo "Reusing base image: $base_image_full"
          else
            echo "Base image not found, building: $base_image_full"
            az acr build \
              --registry "$REGISTRY_NAME" \
              --image "$base_image_name" \
              --file Dockerfile.base \
              . \
              --only-show-errors
          fi

          echo "Building ACI runtime image via az acr build: $RUN_TASK_IMAGE"
          az acr build \
            --registry "$REGISTRY_NAME" \
            --image "$IMAGE_NAME" \
            --file Dockerfile.aci \
            --build-arg BASE_IMAGE="$base_image_full" \
            . \
            --only-show-errors

      - name: Restart DoWhiz services
        run: |
          ssh -F ~/.ssh/config azureproduction << 'EOF'
          set -e
          if [ -s /home/${{ secrets.PROD_AZURE_VM_USER }}/.nvm/nvm.sh ]; then
            source /home/${{ secrets.PROD_AZURE_VM_USER }}/.nvm/nvm.sh
          fi
          APP_DIR=/home/${{ secrets.PROD_AZURE_VM_USER }}/server/DoWhiz/DoWhiz_service
          ENV_FILE="$APP_DIR/.env"
          read_env_value() {
            local key="$1"
            local value
            value="$(grep -E "^${key}=" "$ENV_FILE" | tail -n1 | cut -d= -f2- || true)"
            value="${value#\"}"
            value="${value%\"}"
            value="${value#\'}"
            value="${value%\'}"
            printf '%s' "$value"
          }
          GATEWAY_CONFIG_PATH="$(read_env_value GATEWAY_CONFIG_PATH)"
          EMPLOYEE_CONFIG_PATH="$(read_env_value EMPLOYEE_CONFIG_PATH)"
          [[ -n "$GATEWAY_CONFIG_PATH" ]] || { echo "GATEWAY_CONFIG_PATH is required in .env" >&2; exit 1; }
          [[ -n "$EMPLOYEE_CONFIG_PATH" ]] || { echo "EMPLOYEE_CONFIG_PATH is required in .env" >&2; exit 1; }
          resolve_path() {
            local path="$1"
            if [[ "$path" = /* ]]; then
              echo "$path"
            else
              echo "$APP_DIR/$path"
            fi
          }
          GATEWAY_CONFIG_PATH_RESOLVED="$(resolve_path "$GATEWAY_CONFIG_PATH")"
          EMPLOYEE_CONFIG_PATH_RESOLVED="$(resolve_path "$EMPLOYEE_CONFIG_PATH")"
          [[ -f "$GATEWAY_CONFIG_PATH_RESOLVED" ]] || { echo "Missing gateway config: $GATEWAY_CONFIG_PATH_RESOLVED" >&2; exit 1; }
          [[ -f "$EMPLOYEE_CONFIG_PATH_RESOLVED" ]] || { echo "Missing employee config: $EMPLOYEE_CONFIG_PATH_RESOLVED" >&2; exit 1; }
          [[ "$(basename "$GATEWAY_CONFIG_PATH_RESOLVED")" == "gateway.toml" ]] || {
            echo "Expected production gateway config (gateway.toml), got: $GATEWAY_CONFIG_PATH_RESOLVED" >&2
            exit 1
          }
          [[ "$(basename "$EMPLOYEE_CONFIG_PATH_RESOLVED")" == "employee.toml" ]] || {
            echo "Expected production employee config (employee.toml), got: $EMPLOYEE_CONFIG_PATH_RESOLVED" >&2
            exit 1
          }

          set -a
          source "$ENV_FILE"
          set +a

          default_employee_id="$(awk -F'"' '/^[[:space:]]*default_employee_id[[:space:]]*=/{print $2; exit}' "$EMPLOYEE_CONFIG_PATH_RESOLVED")"
          if [[ -z "${EMPLOYEE_ID:-}" ]]; then
            EMPLOYEE_ID="$default_employee_id"
            export EMPLOYEE_ID
          fi
          [[ -n "${EMPLOYEE_ID:-}" ]] || {
            echo "EMPLOYEE_ID is missing in .env and employee config has no default_employee_id" >&2
            exit 1
          }
          if ! awk -v id="$EMPLOYEE_ID" -F'"' '
            /^[[:space:]]*id[[:space:]]*=/ {
              if ($2 == id) {
                found = 1
              }
            }
            END { exit(found ? 0 : 1) }
          ' "$EMPLOYEE_CONFIG_PATH_RESOLVED"; then
            echo "Configured EMPLOYEE_ID '$EMPLOYEE_ID' not found in $EMPLOYEE_CONFIG_PATH_RESOLVED" >&2
            exit 1
          fi

          echo "Using EMPLOYEE_ID=$EMPLOYEE_ID"
          echo "Using GATEWAY_CONFIG_PATH=$GATEWAY_CONFIG_PATH"
          echo "Using EMPLOYEE_CONFIG_PATH=$EMPLOYEE_CONFIG_PATH"

          # Ensure Azure Files mount is present before starting worker
          # so ACI tasks share workspace/memory paths correctly.
          bash "$APP_DIR/scripts/ensure_aci_share_mount.sh"

          required_bins=(
            "$APP_DIR/target/release/rust_service"
            "$APP_DIR/target/release/inbound_gateway"
          )
          for bin_path in "${required_bins[@]}"; do
            if [[ ! -x "$bin_path" ]]; then
              echo "Missing executable binary: $bin_path" >&2
              ls -al "$APP_DIR/target" 2>/dev/null || true
              ls -al "$APP_DIR/target/release" 2>/dev/null || true
              exit 1
            fi
          done

          dump_pm2_diagnostics() {
            echo "--- pm2 list ---"
            pm2 list || true
            echo "--- pm2 describe dw_worker ---"
            pm2 describe dw_worker || true
            echo "--- pm2 describe dw_gateway ---"
            pm2 describe dw_gateway || true
            echo "--- worker error log ---"
            tail -n 120 "/home/${{ secrets.PROD_AZURE_VM_USER }}/.pm2/logs/dw_worker-error.log" || true
            echo "--- worker out log ---"
            tail -n 120 "/home/${{ secrets.PROD_AZURE_VM_USER }}/.pm2/logs/dw_worker-out.log" || true
            echo "--- gateway error log ---"
            tail -n 120 "/home/${{ secrets.PROD_AZURE_VM_USER }}/.pm2/logs/dw_gateway-error.log" || true
            echo "--- gateway out log ---"
            tail -n 120 "/home/${{ secrets.PROD_AZURE_VM_USER }}/.pm2/logs/dw_gateway-out.log" || true
            echo "--- listening ports 9001/9100 ---"
            ss -ltnp '( sport = :9001 or sport = :9100 )' || true
          }

          for stale_service in dowhiz_rust_service dowhiz-rust-service dowhiz_inbound_gateway dowhiz_gateway dowhiz-inbound-gateway dw_worker dw_gateway; do
            if pm2 describe "$stale_service" >/dev/null 2>&1; then
              pm2 delete "$stale_service" >/dev/null 2>&1 || true
            fi
          done

          export PM2_APP_DIR="$APP_DIR"
          export PM2_KILL_TIMEOUT_MS="${PM2_KILL_TIMEOUT_MS:-300000}"
          export PM2_LISTEN_TIMEOUT_MS="${PM2_LISTEN_TIMEOUT_MS:-15000}"
          pm2 start "$APP_DIR/ecosystem.config.cjs" --only dw_worker,dw_gateway --update-env || {
            dump_pm2_diagnostics
            exit 1
          }

          pm2 save
          pm2 list

          find_pm2_pid() {
            local name
            local pid
            for name in "$@"; do
              pid="$(pm2 pid "$name" | tr -d '[:space:]')"
              if [[ "$pid" =~ ^[1-9][0-9]*$ ]]; then
                echo "$pid"
                return 0
              fi
            done
            return 1
          }

          worker_pid="$(find_pm2_pid dw_worker dowhiz_rust_service dowhiz-rust-service || true)"
          gateway_pid="$(find_pm2_pid dw_gateway dowhiz_inbound_gateway dowhiz_gateway dowhiz-inbound-gateway || true)"
          [[ -n "$worker_pid" ]] || { echo "No running worker PID found after restart" >&2; dump_pm2_diagnostics; exit 1; }
          [[ -n "$gateway_pid" ]] || { echo "No running gateway PID found after restart" >&2; dump_pm2_diagnostics; exit 1; }

          wait_for_health() {
            local label="$1"
            local url="$2"
            local http_code=""
            local attempt
            for attempt in $(seq 1 12); do
              http_code="$(curl -sS -o /dev/null -w '%{http_code}' --connect-timeout 8 --max-time 15 "$url" || true)"
              if [[ "$http_code" == "200" ]]; then
                printf '%s' "$http_code"
                return 0
              fi
              sleep 5
            done
            echo "$label health check failed after retries (HTTP ${http_code:-000})" >&2
            return 1
          }

          worker_health="$(wait_for_health worker "http://127.0.0.1:${RUST_SERVICE_PORT:-9001}/health")" || {
            dump_pm2_diagnostics
            exit 1
          }
          gateway_health="$(wait_for_health gateway "http://127.0.0.1:${GATEWAY_PORT:-9100}/health")" || {
            dump_pm2_diagnostics
            exit 1
          }
          echo "Post-restart health check passed: worker=$worker_health gateway=$gateway_health"
          EOF
