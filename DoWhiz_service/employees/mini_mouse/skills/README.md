Employee-specific skills placeholder for the mini_mouse worker.

This directory is currently inactive because `DoWhiz_service/employee.toml` points `mini_mouse`
at `skills_dir = "skills"`.

Only add skill folders here if you also repoint `skills_dir` to this directory (or merge these
skills into the shared `DoWhiz_service/skills` tree).
