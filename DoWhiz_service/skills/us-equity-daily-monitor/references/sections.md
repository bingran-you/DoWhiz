# Memo Sections

This file is kept as a compatibility pointer.

Use [`output-contract.md`](output-contract.md) as the source of truth for the current section order and labels.

- `Watch Closely` and `Review Now` use the full memo contract.
- `No Material Change` uses the short update contract.
