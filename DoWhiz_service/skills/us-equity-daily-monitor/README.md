# us-equity-daily-monitor sync note

Runtime source of truth lives in `DoWhiz_service/skills/us-equity-daily-monitor`.

At runtime, that directory is copied into task workspaces at `.agents/skills/us-equity-daily-monitor/`.

The checked-in repository copy under `.agents/skills/us-equity-daily-monitor/` is kept as a mirror for local skill work and should be resynced from the runtime copy after edits.
