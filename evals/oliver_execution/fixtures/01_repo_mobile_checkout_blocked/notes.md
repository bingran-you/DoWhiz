# Why This Case Exists

- Repo-derived from the existing website demo thread, so the pack includes at least one case already implied by the shipped product.
- Tests whether Oliver can separate goal, blocker, dependency, and decision instead of flattening them into a generic summary.
- Good failure reveal if readiness comes back yellow or green despite explicit QA and legal blockers.
